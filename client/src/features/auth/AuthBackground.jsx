// Gradient background behind the login/signup card — built from the
// app's existing color variables (--color-primary-dark, --color-accent),
// no external image needed.
export default function AuthBackground() {
  return <div className="auth-background" aria-hidden="true" />;
}